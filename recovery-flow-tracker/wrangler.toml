name = "recovery-flow-tracker"
main = "worker.js"
compatibility_date = "2025-01-01"

# Vars não-secretas. Os secrets (SB_URL, SB_SERVICE_KEY, SHARED_TOKEN)
# vão via `wrangler secret put` — NÃO coloque aqui, esse arquivo é commitado.
[vars]
TABLE_NAME = "manychat_eventos"
